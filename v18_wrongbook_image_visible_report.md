# v18 高数错题本图片显示修复

- 修复对象：高数270道错题本
- 已绑定并强制显示错题本原图：231 道
- 图片缺失路径：0 个

## 关键修复

1. 更新缓存版本为 `20260627-v18-wrongbook-images-visible`，避免浏览器继续读取旧题库。
2. 修改解析显示程序：点击“查看解析”后，错题本原图直接显示在答案卡片内，不再折叠到下面导致看不到。
3. 保留用户上传图片原图，不生成、不改写解析内容。

## 示例

- MATH-0015 已绑定 `question-images/wrongbook-math/MATH-0015-01.png`。
